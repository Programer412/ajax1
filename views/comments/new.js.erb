$("#comment_new").html("<%= j(render 'form', post: @post, comment: @comment) %>");
$("#comment_new").show(); // Make sure the comment form is shown again
$("#show").hide(); // Hide the post and comments table when form is active
